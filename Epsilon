#include <iostream>
using namespace std;

float Mepsilon() {
    float epsilon = 1.0f;  
    while (1.0f + epsilon > 1.0f) {
        epsilon /= 2.0f;  
    }
    return epsilon * 2.0f;
}

int main() {
    float e = Mepsilon();
    cout << e << endl;
    return 0;
}
